diff --git a/templates/base.html b/templates/base.html
index f60c56c..a3f0c38 100644
--- a/templates/base.html
+++ b/templates/base.html
@@ -126,6 +126,8 @@ document.addEventListener('click', function (e) {
     if (!link || !link.closest('.cart-control')) return;
     e.preventDefault();
 
+    const isDecrease = link.classList.contains('dec');
+
     fetch(link.href, { headers: { 'X-Requested-With': 'XMLHttpRequest' } })
         .then(res => res.json())
         .then(data => {
@@ -142,8 +144,15 @@ document.addEventListener('click', function (e) {
             }
             document.getElementById('cart-badge').textContent = data.cart_count;
 
+            let message;
+            if (isDecrease) {
+                message = data.quantity > 0 ? 'Quantity updated' : 'Product removed from cart';
+            } else {
+                message = 'Product added to cart';
+            }
+
             const toast = document.getElementById('toast');
-            toast.textContent = 'Product added to cart';
+            toast.textContent = message;
             toast.classList.add('show');
             setTimeout(() => toast.classList.remove('show'), 1800);
         });
